# Market Intelligence implementation checkpoint

Working log for the Market Intelligence milestone. Updated as work lands so the session can
resume after context compaction. Operator reference and capability statuses live in
`docs/MARKET_INTELLIGENCE.md`; this file records what was actually run and found.

## Branches and review anchors

| Repo | Branch | Base | Reviewed head (anchor) |
|------|--------|------|------------------------|
| FMP_SCREENER | `cursor/market-intelligence-v1-674b` (PR #18) | `main` @ `9d9d98708c06ef292cb663d91775cb8b44e1c05b` | `41011a1e82268001146969ac748b9d446f728487` |
| quant-strategies | `cursor/platform-market-intelligence-674b` (PR #27) | PR #26 `cursor/platform-research-lifecycle-674b` @ `38612911f1a4613647951c71962a56aebbf98959` | `479af284f83a071a45750aeda79de9703b58d7e9` |

Merge order: QS #26 -> QS #27; FMP #18 is independent of QS at the code level (the consumer reads
artifact files) but the two contract fixtures were generated by FMP `ideas.py` at this head.

Workflow trigger audit (re-done before every push):

- FMP: `deploy.yml` (push `main` only), `ingest_platform_research.yml` (repository_dispatch /
  hourly schedule / manual), `ingest_tlt_duration_momentum.yml` (manual), `platform_research_verify.yml`
  + `stage1_verify.yml` (workflow_run after deploy), new `pr_validation.yml` (`pull_request`,
  `workflow_dispatch`; no secrets). No push trigger matches `cursor/market-intelligence-v1-674b`.
- QS: push patterns `smoke/**`, `stage1/**`, `bootstrap-research/**`, `smoke/PlatformResearch/**`,
  `cursor/platform-qc-*-674b`, `cursor/platform-research-tlt*`, `stage2-smoke/**`,
  `cursor/stage2-smoke-*-674b`, `stage2-window/**`, `cursor/stage2-window-*-674b`, `stage2-suite/**`,
  `cursor/stage2-suite-674b`, `cursor/stage2-suite-*-674b`, `cursor/stage2-inspect-*-674b`;
  `publish_platform_research.yml` fires on pushes touching `research/platform_smokes/**` or
  `research/platform_artifacts/**`. `cursor/platform-market-intelligence-674b` matches none and the
  branch never touches those paths (`git diff --name-only 3861291..HEAD` = `research/market_intelligence/**`,
  `tests/**`, `.github/workflows/pr_validation.yml`). New `pr_validation.yml`: `pull_request` only, no secrets.

## Environment used for validation

- Build host: Python 3.12.3, PostgreSQL 16.15 (`psql` 16.15) local disposable instance exported as
  `FMP_TEST_DATABASE_URL` (test-only role; the session fixture creates and drops `fmp_mi_test_<uuid>`
  databases). `systemd-analyze` available for timer/unit verification.
- Not available (so `CONFIGURATION_REQUIRED`, not validated): `FRED_API_KEY`, FMP `outputs/precomputed`
  bundles, production `DATABASE_URL`, `DATABASE_READONLY_URL`, `AI_CONTEXT_API_TOKEN`, QC credentials.
  No provider call, production read, or QC action was attempted.

## Review findings -> status (evidence in tests)

| Finding | Status | Evidence |
|---|---|---|
| WTREGEN/WRESBAL billions vs official millions; WTREGEN "Wednesday level" vs weekly average ending Wednesday | FIXED (`fred_catalog_v2`); whole catalog re-verified against `tests/fixtures/fred_official_metadata.json` (independent of the catalog module) | `tests/test_mi_contracts.py`, `tests/test_mi_ingest_bridge.py` |
| `validate_metadata` MISMATCH but observations written and success recorded; analytics used catalog units without conversion | FIXED: publication gate + quarantine, `METADATA_REJECTED` transport, last valid data kept, other series continue; `display_scale_v1` conversion with raw units retained | `test_mi_ingest_bridge.py`, `test_mi_pipeline.py` (quarantine summary view) |
| Synthetic fixtures repeated the catalog assumptions | FIXED: official-metadata fixture is independent of `catalog.py`; missing metadata never validates | `tests/mi_fixtures.py::official_metadata` |
| `export_safe_payload` hash computed before `latest_snapshot`/`available`/`degraded` appended; test stripped fields | FIXED: `build_envelope` + `finalize_envelope`; hash covers whole envelope minus digest; HTTP JSON verified untouched; tampering and missing-snapshot cases | `test_mi_pipeline.py` API tests |
| `filter_for_export` passed `{"export_scope":"INTERNAL_ONLY","value":123}` | FIXED: explicit per-scope behaviour, unknown/missing scope fails closed, nested/derived/order redaction | `test_mi_pipeline.py::test_*redact*`, `test_mi_contracts.py` |
| Live section queries labelled with the latest snapshot hash | FIXED: sections served from the frozen snapshot; `/v1/context/data-health/live` is explicitly live with its own hash (`/data-health` alias kept) | `ai_context_api.py`, HTTP tests |
| Morning builder: `cutoff_at` from supplied `generated_at`, unconstrained latest views, `as_of` only a parameter | FIXED (`morning_context_v2`): current-only, DB transaction clock, exact `input_refs`, `content_sha256`, supersession, past `--cutoff` refused (`HistoricalReconstructionUnsupported`) | `test_mi_pipeline.py` morning tests |
| Freshness frozen between ingestion runs; MIXED cadence shown per row; tolerance shown as release date | FIXED: read-time assessment against the reader clock, per-dataset cadence, `stale_after_estimate` wording, UI cache TTL | `test_mi_pipeline.py::test_*clock*`, pages tests |
| Analytics only for latest observation date -> one-point charts | FIXED: bounded idempotent backfill + revision-aware incremental recompute; first-run charts populated from stored history | `test_mi_pipeline.py` (`HISTORY_START` backfill), `test_mi_ingest_bridge.py` |
| Legacy bridge coverage (252d drawdown with 20 obs, NULL compression, bundle-wide latest date), quarantine not affecting health, hash not over stored body | FIXED (`legacy_bridge_v2`) | `test_mi_ingest_bridge.py`, `test_mi_pipeline.py` (STALE instruments) |
| Read-only role tested only inside READ ONLY transaction; PUBLIC grants | FIXED: real role via psql from the shipped SQL, denial with defaults overridden, PUBLIC CREATE remediation documented and tested; grants-vs-views coverage test | `test_mi_pipeline.py` role tests, `test_mi_schema_store.py` |
| Bond YTM 200% / repriced 26.875 with FULL support; no bracketing/convergence checks | FIXED (`bond_analytics_v2`): bracket + convergence + repricing tolerance, `UNSUPPORTED_*` statuses, first-coupon stubs honoured, LAST never MID, Z-spread only with documented curve | `tests/test_bond_analytics.py::test_reviewed_defect_price_1_on_one_year_bond_is_out_of_domain_not_200pct_yield` and siblings |
| Idea contracts: no completeness gate, string-token date warnings, idea holdout loosened to 2025, support statuses overstated | FIXED (`idea_spec_v2` / contract v2, migration 013): approval requires COMPLETE + human + hash binding; structured `date_ranges`; `effective_holdout_start` = min(idea, platform); `MANUAL_SPEC_REQUIRED` / `UNSUPPORTED` honest statuses; duplicate spec refused | `tests/test_research_ideas.py`; QS `tests/test_market_intelligence_research.py` |
| PR #27 was an ETF RS diagnostic, not a PIT sector producer | FIXED locally: `sector_internals_v1` producer (PIT membership calendar, prices, optional PIT caps; trailing vs held returns; NULL not substitution; constituent data never exported) + FMP consumer (migration 014, validation matrix, revisions, date gate, page 17). QC export project remains NOT_IMPLEMENTED, activation DISABLED_BY_POLICY | QS `test_market_intelligence_research.py`; FMP `tests/test_mi_pit_sector.py` |
| RS condition (5/63/21 defaults) promoted as infrastructure | FIXED: windows come only from the frozen spec (`windows_from_spec`); no defaults; labelled hypothesis | QS `test_hypothesis_windows_come_only_from_the_frozen_spec` |
| No secretless PR validation; DB checks could silently skip | FIXED: `pr_validation.yml` in both repos (`pull_request`, no secrets, postgres:16 service, `MI_REQUIRE_DB_TESTS=1` fails instead of skipping, clean + no-op migration step) | `tests/test_mi_deploy.py`, QS CI audit tests |
| Delivery fallback labelling | ALREADY FIXED at reviewed head (`LAST_KNOWN_GOOD` in `delivery_visibility.py`); verified `research/platform_smokes` absent on QS `main` so scheduled pulls report `LAST_KNOWN_GOOD` until `QS_ARTIFACT_SOURCE_REF` is set | `tests/test_delivery_visibility.py` |

Independent findings fixed along the way (not in the review list): `pd.NaT` serialised as
"NaT"; zero-curve bootstrap skipped periods below the shortest tenor; env example told operators to
pass the read-only password in `-v ...=` argv with the wrong variable name (now protected-file form);
`stage2` migration list test and schema tests updated for 013/014; registry entry for
`QC_MARKET_INTELLIGENCE` described a dataset (`sector_diagnostics_v1`) that never existed as an
ingest path — now `sector_internals_v1` with honest terms.

## Commits since the reviewed heads

FMP (`41011a1..HEAD`): `255aefd` catalog v2 + publication gate; `43d4a2f` analytics backfill /
recompute; `179f3eb` morning v2; `147aa35` export v2 + API contract + live clock + readiness;
`a351e5a` pages; `15cc602` tests; `fa904fb` legacy bridge v2; `06fa786` upgrade-path test;
`1744dc4` bonds v2; `fb3c892` ideas v2 + migration 013; `2db94fb` PIT sector consumer + migration
014 + page 17; `9f84795` CI + ops verification; docs commit (this file).

QS (`479af28..HEAD`): `fa2ecad` contract v2 consumer + `sector_internals_v1` producer + frozen-spec
windows; `32c9ce7` fixture provenance; `a8e2b07` offline PR validation workflow + trigger audit tests;
`55247fd` README statuses; `df2a792` pandas declared as a test-only dependency (two pre-existing tests
imported it undeclared) + protected-path diff base from CI; `bb03224` base ref from full-history checkout.

## Validation results (final heads)

- FMP: `FMP_TEST_DATABASE_URL=... MI_REQUIRE_DB_TESTS=1 python3 -m pytest -q -p no:cacheprovider`
  -> **371 passed** (PostgreSQL 16.15). Without the URL and with `MI_REQUIRE_DB_TESTS=1` the same
  command fails 126 DB tests with "DB checks must not silently skip in CI" (fail-closed guard verified).
- QS: `python3 -m pytest -q -p no:cacheprovider` with QC variables unset -> **502 passed** (500 + 2
  CI audit tests).
- Protected paths: `sha256sum` of the 66 files under `stage1_results/`, `stage2_results/`,
  `qc_research/platform_artifacts/` identical to `origin/main` (`/tmp/protected_hashes_{main,after}.txt`
  diff empty); TLT fingerprint `d8f43c83ddec8d70` present and unchanged. `qc_research/*.py` unchanged
  since the reviewed head. QS: no file outside `research/market_intelligence/**`, `tests/**`,
  `.github/workflows/pr_validation.yml` changed since PR #26's head.
- CI (first real runs on the pushed heads): FMP `PR validation (offline + disposable PostgreSQL)` at
  `5131cb4` success — offline job 245 passed / 126 skipped (DB tests skip without a database, as
  designed), postgres job 371 passed on PostgreSQL 16.15 (Debian container) + clean apply of 14
  migrations + no-op reapply + 30-test DB probe with zero skips. QS `PR validation (offline tests)` at
  `bb03224` success — 502 passed, protected-path diff test executed against `origin/cursor/platform-research-lifecycle-674b`.
  No other workflow ran on either branch (no QC, no deploy, no publish).
- Timer templates: `systemd-analyze calendar` EST/EDT/weekend cases and `systemd-analyze verify` on
  rendered units pass (`tests/test_mi_deploy.py`).
- Not run (by instruction / no access): QuantConnect backtests of any kind, FRED live calls, legacy
  bundle parity on real files, production DB reads, timer `--apply`, role provisioning, API exposure.

## Remaining gates (human)

- `CONFIGURATION_REQUIRED`: `FRED_API_KEY`, `DATABASE_READONLY_URL` (after `mi_readonly` is
  provisioned), `AI_CONTEXT_API_TOKEN`, writer identity in `/etc/fmp/market_intelligence.env`,
  repository variables `QS_ARTIFACT_SOURCE_REF` / `QS_ARTIFACT_SOURCE_PATH`.
- `ENTITLEMENT_REQUIRED`: external redistribution of ICE BofA and FMP/QC-derived sector values.
- `DISABLED_BY_POLICY`: timer `--apply`, QC activation (`MI_RESEARCH_ACTIVATION_APPROVED`), any
  backtest from an idea, IBKR/TRACE adapters, ML_FINAL_HOLDOUT.
- `NOT_IMPLEMENTED`: QC research project exporting real PIT membership/prices/caps for the
  `sector_internals_v1` producer; OAS / floaters; bond terms/quotes ingestion source.
