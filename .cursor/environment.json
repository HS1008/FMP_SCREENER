{
  "name": "FMP Screener",
  "user": "ubuntu",
  "install": "cd /agent/repos/fmp_screener && bash .cursor/install.sh",
  "terminals": [
    {
      "name": "api",
      "command": "cd /agent/repos/fmp_screener && . .venv/bin/activate && uvicorn api_app:app --host 127.0.0.1 --port 8765",
      "description": "FastAPI JSON service (health at /health, OpenAPI docs at /docs). Data routes need FMP_API_KEY."
    },
    {
      "name": "dashboard",
      "command": "cd /agent/repos/fmp_screener && . .venv/bin/activate && streamlit run dashboard.py",
      "description": "Streamlit sector dashboard on port 8501. Data tabs need FMP_API_KEY."
    }
  ],
  "ports": [
    { "name": "dashboard", "port": 8501 },
    { "name": "api", "port": 8765 }
  ]
}
